spring:
#  config:
#    import:
#      - classpath:env.yml
  profiles:
    active: oauth,db
jwt:
  secret: ${JWT_SECRET}
  url: "http://py:5000/jwt"
  issue: "/issue"
  logout: "/logout"
  reissue: "/reissue"
  redirect_url: "http://localhost:3000"
  validTime: 1800000
  refreshTime: 3600000
